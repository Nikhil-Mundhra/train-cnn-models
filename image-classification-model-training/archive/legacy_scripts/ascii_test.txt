```text
Input → Conv1 → MaxPool → ResBlock 1 → ResBlock 2
                                            ↓
 Output ← FC Head ← GAP ← ResBlock 4 ← ResBlock 3
```

```text
Input → Stem → MBConv 1 → MBConv 2
                              ↓
Output ← FC Head ← Head ← MBConv 3
```

```text
High-Res Input → Stem → MBConv 1 → MBConv 2
                                       ↓
                        Output ← FC Head ← Head
```

